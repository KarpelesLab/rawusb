# rawusb

Dependency-free, cross-platform USB device access for Rust, in the spirit of
libusb: device enumeration, descriptors, synchronous and asynchronous
transfers, with direct access to the low-level transfer machinery (allocate,
submit, cancel) when the convenience layer is not enough.

Work in progress.
